# Fixtures

Place test data and token files here.

- `tokens/` — API tokens and auth fixtures (never commit real secrets; use environment variables or a secrets manager)
- `data/` — seed data for Apache AGE graph test scenarios
