import { Given, When, Then } from '@cucumber/cucumber';

Given('a user authenticated with the trader role', function () {
  return 'pending';
});

Given('a valid Private Debt deal payload', function () {
  return 'pending';
});

When('the payload is submitted to MTCM', function () {
  return 'pending';
});

Then('the deal is created with status {string}', function (_status: string) {
  return 'pending';
});

Then('the response includes a deal id', function () {
  return 'pending';
});
